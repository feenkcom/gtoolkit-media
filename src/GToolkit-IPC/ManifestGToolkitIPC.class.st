"
GToolkit-IPC (Inter-Process Communications) loads the dependencies for communicating with other Pharo images using TelePharo & Seamless and provides convience classes for managing worker image pools, startup and shutdown.
"
Class {
	#name : #ManifestGToolkitIPC,
	#superclass : #PackageManifest,
	#category : #'GToolkit-IPC-Manifest'
}
